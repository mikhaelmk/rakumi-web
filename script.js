const translations = {
  en: {
    "meta.title": "Rakumi — Building things under the moon",
    "meta.description": "Rakumi — code, stories and experiments built under the moon.",
    "nav.aria": "Primary navigation",
    "nav.about": "About",
    "nav.projects": "Projects",
    "nav.building": "Building",
    "language.aria": "Language",
    "hero.eyebrow": "CODE · STORIES · EXPERIMENTS",
    "hero.title": "Building things<br />under the moon.",
    "hero.text": "Rakumi is a quiet corner of the internet for projects, strange ideas, stories and code built one step at a time.",
    "hero.explore": "Explore my work",
    "hero.statusAria": "Rakumi status",
    "hero.status": "signal online",
    "hero.orbit": "orbit / 2026",
    "hero.commits": "small commits, strange ideas",
    "hero.alt": "Rakumi, a lunar raccoon sitting on a crescent moon",
    "about.eyebrow": "01 — ABOUT",
    "about.title": "A small universe in progress.",
    "about.text": "Rakumi is my little corner of the internet — a place for code, stories and experiments built one project at a time.",
    "about.noteLabel": "// field note",
    "about.note": "Nothing here needs to be enormous. It only needs to keep moving.",
    "about.orbitAria": "The three Rakumi project paths",
    "about.oneIdentity": "one identity",
    "about.code": "code",
    "about.story": "story",
    "about.channel": "channel",
    "projects.eyebrow": "02 — THE THREE PROJECTS",
    "projects.title": "Three paths. One identity.",
    "projects.currentPhase": "Current phase",
    "projects.ordo.status": "In Development",
    "projects.ordo.kind": "CODE / 01",
    "projects.ordo.description": "A coding project being built release by release, with the first functional version focused on a small, understandable core.",
    "projects.ordo.phase": "v0.1 — foundation",
    "projects.ordo.link": "Open GitHub →",
    "projects.station.status": "Writing",
    "projects.station.kind": "STORY / 02",
    "projects.station.title": "Untitled Station Novel",
    "projects.station.description": "A liminal railway station, an enormous moon and a story about identity, time and fragments of a life.",
    "projects.station.phase": "manuscript in progress",
    "projects.station.link": "Follow the manuscript →",
    "projects.channel.status": "Preparing",
    "projects.channel.kind": "CHANNEL / 03",
    "projects.channel.title": "Rakumi Channel",
    "projects.channel.description": "Videos about technology, projects, games, ideas and the things being created inside the Rakumi universe.",
    "projects.channel.terminal": "transmission_preparing",
    "projects.channel.link": "Visit Channel →",
    "building.eyebrow": "03 — CURRENTLY BUILDING",
    "building.title": "What is moving right now.",
    "building.project": "Project",
    "building.statusLabel": "Status",
    "building.status": "Building",
    "building.nextMilestone": "Next milestone",
    "building.milestone": "v0.1 — create & list tasks",
    "timeline.eyebrow": "04 — TIMELINE",
    "timeline.identityLabel": "identity",
    "timeline.identity": "Rakumi created",
    "timeline.codeLabel": "code",
    "timeline.code": "Ordo — v0.1 in development",
    "timeline.storyLabel": "story",
    "timeline.story": "Station manuscript in progress",
    "timeline.channelLabel": "channel",
    "timeline.channel": "Rakumi transmission preparing for launch",
    "timeline.signoff": "same moon / more possibilities",
    "footer.tagline": "Building things under the moon.",
    "footer.status": "online · 2026"
  },
  pt: {
    "meta.title": "Rakumi — Construindo coisas sob a lua",
    "meta.description": "Rakumi — código, histórias e experimentos construídos sob a lua.",
    "nav.aria": "Navegação principal",
    "nav.about": "Sobre",
    "nav.projects": "Projetos",
    "nav.building": "Em construção",
    "language.aria": "Idioma",
    "hero.eyebrow": "CÓDIGO · HISTÓRIAS · EXPERIMENTOS",
    "hero.title": "Construindo coisas<br />sob a lua.",
    "hero.text": "Rakumi é um canto tranquilo da internet para projetos, ideias estranhas, histórias e código construídos um passo de cada vez.",
    "hero.explore": "Explorar meus projetos",
    "hero.statusAria": "Status da Rakumi",
    "hero.status": "sinal online",
    "hero.orbit": "órbita / 2026",
    "hero.commits": "pequenos commits, ideias estranhas",
    "hero.alt": "Rakumi, um guaxinim lunar sentado sobre uma lua crescente",
    "about.eyebrow": "01 — SOBRE",
    "about.title": "Um pequeno universo em construção.",
    "about.text": "Rakumi é o meu pequeno canto da internet — um lugar para código, histórias e experimentos construídos um projeto de cada vez.",
    "about.noteLabel": "// nota de campo",
    "about.note": "Nada aqui precisa ser enorme. Só precisa continuar avançando.",
    "about.orbitAria": "Os três caminhos de projetos da Rakumi",
    "about.oneIdentity": "uma identidade",
    "about.code": "código",
    "about.story": "história",
    "about.channel": "canal",
    "projects.eyebrow": "02 — OS TRÊS PROJETOS",
    "projects.title": "Três caminhos. Uma identidade.",
    "projects.currentPhase": "Fase atual",
    "projects.ordo.status": "Em desenvolvimento",
    "projects.ordo.kind": "CÓDIGO / 01",
    "projects.ordo.description": "Um projeto de programação construído versão por versão, com a primeira entrega funcional concentrada em um núcleo pequeno e compreensível.",
    "projects.ordo.phase": "v0.1 — fundação",
    "projects.ordo.link": "Abrir GitHub →",
    "projects.station.status": "Escrevendo",
    "projects.station.kind": "HISTÓRIA / 02",
    "projects.station.title": "Romance da Estação — sem título",
    "projects.station.description": "Uma estação ferroviária liminar, uma lua enorme e uma história sobre identidade, tempo e fragmentos de uma vida.",
    "projects.station.phase": "manuscrito em andamento",
    "projects.station.link": "Acompanhar o manuscrito →",
    "projects.channel.status": "Preparando",
    "projects.channel.kind": "CANAL / 03",
    "projects.channel.title": "Canal Rakumi",
    "projects.channel.description": "Vídeos sobre tecnologia, projetos, jogos, ideias e as coisas que estão sendo criadas dentro do universo Rakumi.",
    "projects.channel.terminal": "transmissao_preparando",
    "projects.channel.link": "Visitar canal →",
    "building.eyebrow": "03 — EM CONSTRUÇÃO",
    "building.title": "O que está avançando agora.",
    "building.project": "Projeto",
    "building.statusLabel": "Status",
    "building.status": "Construindo",
    "building.nextMilestone": "Próximo marco",
    "building.milestone": "v0.1 — criar e listar tarefas",
    "timeline.eyebrow": "04 — LINHA DO TEMPO",
    "timeline.identityLabel": "identidade",
    "timeline.identity": "Rakumi criada",
    "timeline.codeLabel": "código",
    "timeline.code": "Ordo — v0.1 em desenvolvimento",
    "timeline.storyLabel": "história",
    "timeline.story": "Manuscrito da estação em andamento",
    "timeline.channelLabel": "canal",
    "timeline.channel": "Transmissão Rakumi sendo preparada para lançamento",
    "timeline.signoff": "a mesma lua / mais possibilidades",
    "footer.tagline": "Construindo coisas sob a lua.",
    "footer.status": "online · 2026"
  }
};

const SUPPORTED_LANGUAGES = ["en", "pt"];
const LANGUAGE_STORAGE_KEY = "rakumi-language";
const languageButtons = document.querySelectorAll("[data-lang]");
const metaDescription = document.querySelector('meta[name="description"]');

function t(language, key) {
  return translations[language]?.[key] ?? translations.en[key] ?? key;
}

function applyLanguage(language, { persist = true } = {}) {
  const nextLanguage = SUPPORTED_LANGUAGES.includes(language) ? language : "en";

  document.documentElement.lang = nextLanguage === "pt" ? "pt-BR" : "en";
  document.title = t(nextLanguage, "meta.title");
  if (metaDescription) metaDescription.content = t(nextLanguage, "meta.description");

  document.querySelectorAll("[data-i18n]").forEach((element) => {
    element.textContent = t(nextLanguage, element.dataset.i18n);
  });

  document.querySelectorAll("[data-i18n-html]").forEach((element) => {
    element.innerHTML = t(nextLanguage, element.dataset.i18nHtml);
  });

  document.querySelectorAll("[data-i18n-aria-label]").forEach((element) => {
    element.setAttribute("aria-label", t(nextLanguage, element.dataset.i18nAriaLabel));
  });

  document.querySelectorAll("[data-i18n-alt]").forEach((element) => {
    element.setAttribute("alt", t(nextLanguage, element.dataset.i18nAlt));
  });

  languageButtons.forEach((button) => {
    const isActive = button.dataset.lang === nextLanguage;
    button.classList.toggle("is-active", isActive);
    button.setAttribute("aria-pressed", String(isActive));
  });

  if (persist) localStorage.setItem(LANGUAGE_STORAGE_KEY, nextLanguage);
}

languageButtons.forEach((button) => {
  button.addEventListener("click", () => applyLanguage(button.dataset.lang));
});

const storedLanguage = localStorage.getItem(LANGUAGE_STORAGE_KEY);
const browserLanguage = navigator.language?.toLowerCase().startsWith("pt") ? "pt" : "en";
applyLanguage(storedLanguage || browserLanguage, { persist: Boolean(storedLanguage) });

const progressBars = document.querySelectorAll(".progress-bar");

const revealProgress = (bar) => {
  const value = bar.dataset.progress || "0";
  bar.style.width = `${value}%`;
};

if ("IntersectionObserver" in window) {
  const observer = new IntersectionObserver(
    (entries, instance) => {
      entries.forEach((entry) => {
        if (!entry.isIntersecting) return;

        revealProgress(entry.target);
        instance.unobserve(entry.target);
      });
    },
    { threshold: 0.35 }
  );

  progressBars.forEach((bar) => observer.observe(bar));
} else {
  progressBars.forEach(revealProgress);
}

/* =========================
   V4 — Interactive orbital navigator
   ========================= */

const orbitSystem = document.querySelector("[data-orbit-system]");

if (orbitSystem) {
  const stage = orbitSystem.querySelector("[data-orbit-stage]");
  const marker = orbitSystem.querySelector("[data-orbit-marker]");
  const rings = [...orbitSystem.querySelectorAll("[data-orbit-ring]")];
  const planets = [...orbitSystem.querySelectorAll("[data-orbit-planet]")];
  const modeButtons = [...orbitSystem.querySelectorAll("[data-orbit-mode]")];
  const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  const CENTER = { x: 310, y: 210 };
  const MODE_DURATION = 720;

  const modes = {
    ordo: [
      { rx: 252, ry: 104, rot: -2, speed: 0.000020 },
      { rx: 207, ry: 84, rot: -2, speed: -0.000026 },
      { rx: 162, ry: 66, rot: -2, speed: 0.000033 },
      { rx: 119, ry: 49, rot: -2, speed: -0.000041 },
      { rx: 82, ry: 34, rot: -2, speed: 0.000050 }
    ],
    station: [
      { rx: 252, ry: 122, rot: 13, speed: 0.000018 },
      { rx: 210, ry: 96, rot: 9, speed: -0.000024 },
      { rx: 165, ry: 78, rot: 16, speed: 0.000031 },
      { rx: 122, ry: 59, rot: 7, speed: -0.000039 },
      { rx: 84, ry: 40, rot: 18, speed: 0.000048 }
    ],
    rakumi: [
      { rx: 254, ry: 116, rot: -7, speed: 0.000019 },
      { rx: 210, ry: 94, rot: -5, speed: -0.000025 },
      { rx: 166, ry: 76, rot: -9, speed: 0.000032 },
      { rx: 124, ry: 57, rot: -4, speed: -0.000040 },
      { rx: 86, ry: 39, rot: -11, speed: 0.000049 }
    ]
  };

  const planetMap = [
    { orbit: 0, angle: 0.54 },
    { orbit: 0, angle: 2.78 },
    { orbit: 1, angle: 4.02 },
    { orbit: 1, angle: 0.94 },
    { orbit: 2, angle: 3.45 },
    { orbit: 4, angle: 5.57 }
  ];

  let activeMode = orbitSystem.dataset.mode || "rakumi";
  let previousTime = performance.now();
  let transitionStart = previousTime;
  let transitionFrom = modes[activeMode].map((item) => ({ ...item }));
  let transitionTo = modes[activeMode].map((item) => ({ ...item }));
  let current = modes[activeMode].map((item) => ({ ...item }));

  let lockedPointer = { x: 0, y: 0 };
  let previewPointer = { x: 0, y: 0 };
  let previewing = false;

  const clamp = (value, min, max) => Math.min(max, Math.max(min, value));
  const lerp = (start, end, amount) => start + (end - start) * amount;
  const ease = (value) => 1 - Math.pow(1 - value, 3);

  function interpolateConfig(time) {
    const raw = reduceMotion ? 1 : clamp((time - transitionStart) / MODE_DURATION, 0, 1);
    const amount = ease(raw);

    current = transitionFrom.map((from, index) => {
      const to = transitionTo[index];
      return {
        rx: lerp(from.rx, to.rx, amount),
        ry: lerp(from.ry, to.ry, amount),
        rot: lerp(from.rot, to.rot, amount),
        speed: lerp(from.speed, to.speed, amount)
      };
    });
  }

  function ellipsePoint(config, angle) {
    const x = Math.cos(angle) * config.rx;
    const y = Math.sin(angle) * config.ry;
    const rotation = config.rot * Math.PI / 180;

    return {
      x: CENTER.x + (x * Math.cos(rotation) - y * Math.sin(rotation)),
      y: CENTER.y + (x * Math.sin(rotation) + y * Math.cos(rotation))
    };
  }

  function renderOrbit(time) {
    const delta = Math.min(time - previousTime, 40);
    previousTime = time;
    interpolateConfig(time);

    rings.forEach((ring, index) => {
      const config = current[index];
      ring.setAttribute("rx", config.rx.toFixed(2));
      ring.setAttribute("ry", config.ry.toFixed(2));
      ring.setAttribute("transform", `rotate(${config.rot.toFixed(2)} ${CENTER.x} ${CENTER.y})`);
    });

    planetMap.forEach((planet, index) => {
      if (!reduceMotion) {
        planet.angle += current[planet.orbit].speed * delta;
      }

      const point = ellipsePoint(current[planet.orbit], planet.angle);
      const body = planets[index];
      body.setAttribute("cx", point.x.toFixed(2));
      body.setAttribute("cy", point.y.toFixed(2));
    });

    requestAnimationFrame(renderOrbit);
  }

  function pointerRatio(event) {
    const rect = stage.getBoundingClientRect();
    return {
      x: clamp(((event.clientX - rect.left) / rect.width) * 2 - 1, -1, 1),
      y: clamp(((event.clientY - rect.top) / rect.height) * 2 - 1, -1, 1),
      localX: event.clientX - rect.left,
      localY: event.clientY - rect.top
    };
  }

  function applyStageTransform(pointer, strength = 1) {
    if (reduceMotion) return;

    const tiltX = (-pointer.y * 6.5 * strength).toFixed(2);
    const tiltY = (pointer.x * 8.5 * strength).toFixed(2);
    const shiftX = (pointer.x * 7 * strength).toFixed(2);
    const shiftY = (pointer.y * 5 * strength).toFixed(2);

    stage.style.setProperty("--orbit-tilt-x", `${tiltX}deg`);
    stage.style.setProperty("--orbit-tilt-y", `${tiltY}deg`);
    stage.style.setProperty("--orbit-shift-x", `${shiftX}px`);
    stage.style.setProperty("--orbit-shift-y", `${shiftY}px`);
  }

  function showMarker(localX, localY) {
    if (!marker || reduceMotion) return;

    marker.style.left = `${localX}px`;
    marker.style.top = `${localY}px`;
    marker.classList.remove("is-visible");
    void marker.offsetWidth;
    marker.classList.add("is-visible");
  }

  function setMode(mode) {
    if (!modes[mode]) return;

    activeMode = mode;
    orbitSystem.dataset.mode = mode;
    transitionFrom = current.map((item) => ({ ...item }));
    transitionTo = modes[mode].map((item) => ({ ...item }));
    transitionStart = performance.now();

    modeButtons.forEach((button) => {
      const isActive = button.dataset.orbitMode === mode;
      button.classList.toggle("is-active", isActive);
      button.setAttribute("aria-pressed", String(isActive));
    });
  }

  modeButtons.forEach((button) => {
    button.addEventListener("click", () => setMode(button.dataset.orbitMode));
  });

  stage.addEventListener("pointermove", (event) => {
    if (event.pointerType === "touch" || reduceMotion) return;
    const point = pointerRatio(event);
    previewPointer = { x: point.x, y: point.y };
    previewing = true;
    stage.classList.add("is-pointing");
    applyStageTransform(previewPointer, 0.58);
  });

  stage.addEventListener("pointerleave", () => {
    previewing = false;
    stage.classList.remove("is-pointing");
    applyStageTransform(lockedPointer, 1);
  });

  stage.addEventListener("pointerdown", (event) => {
    if (reduceMotion || event.target.closest("[data-orbit-mode]")) return;
    const point = pointerRatio(event);
    lockedPointer = { x: point.x, y: point.y };
    previewPointer = lockedPointer;
    previewing = false;

    stage.classList.remove("is-pointing");
    stage.classList.add("is-clicked");
    applyStageTransform(lockedPointer, 1);
    showMarker(point.localX, point.localY);

    window.setTimeout(() => stage.classList.remove("is-clicked"), 480);
  });

  window.addEventListener("blur", () => {
    if (!previewing) applyStageTransform(lockedPointer, 1);
  });

  setMode(activeMode);
  requestAnimationFrame(renderOrbit);
}
